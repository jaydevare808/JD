<resources>
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">#405DE6</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">#172033</item>
        <item name="android:navigationBarColor">#172033</item>
        <!-- Android 15 compatibility for the API 36 stability candidate. Android 16 ignores this opt-out. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
</resources>
